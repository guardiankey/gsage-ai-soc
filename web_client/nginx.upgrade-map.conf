# Map directive for WebSocket / SSE Connection upgrade.
# Loaded at http{} context because /etc/nginx/conf.d/*.conf is included there.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
