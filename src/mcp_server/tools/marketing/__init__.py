"""gSage AI — marketing automation tool families.

Tools in this namespace wrap external marketing platforms (e-mail / SMS /
contact-list management). Each subpackage targets a single platform and is
auto-discovered by :func:`src.mcp_server.registry.registry.build_registry`.
"""
