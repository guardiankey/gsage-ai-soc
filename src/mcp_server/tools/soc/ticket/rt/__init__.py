"""gSage AI — Request Tracker (RT) integration tools.

Provides read and write tools for Best Practical Request Tracker (RT) via
the REST 2.0 API. All tools share the ``rt`` config namespace.

Tools:
    * ``rt_search`` — TicketSQL / structured filter search
    * ``rt_get_ticket`` — enriched single-ticket view
    * ``rt_dashboard`` — managerial aggregations
    * ``rt_manage`` — write actions (HITL approval required)
"""
