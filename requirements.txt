aiohttp>=3.9,<4
aioimaplib>=1.0,<2
aiosmtplib>=3.0,<4
aiomysql>=0.2,<1        # async MySQL/MariaDB driver (SQLAlchemy: mysql+aiomysql://)
aioodbc>=0.5,<1         # async ODBC for SQL Server (SQLAlchemy: mssql+aioodbc://)
asyncpg>=0.29,<1
asyncssh>=2.14,<3
oracledb>=2.0,<3        # Oracle Database driver, async-capable (SQLAlchemy: oracle+oracledb://)
celery[redis]>=5.3,<6
dnspython>=2.6,<3
cryptography>=42.0,<47
elasticsearch[async]>=8.13,<9
fastapi>=0.111,<1
httpx>=0.27,<1
bcrypt>=4.2,<5
PyJWT>=2.8,<3
python-telegram-bot>=21.0,<22
# Microsoft Bot Framework + MSAL — used by the Teams channel handler
botbuilder-core>=4.16,<5
botbuilder-schema>=4.16,<5
msal>=1.28,<2
pydantic[email]>=2.7,<3
pydantic-settings>=2.2,<3
psycopg2-binary>=2.9,<3
python-multipart>=0.0.9
redis>=5.0,<6
celery-redbeat>=2.2,<3
sqlalchemy[asyncio]>=2.0,<3
alembic>=1.13,<2
tiktoken>=0.7,<1
uvicorn[standard]>=0.29,<1
gunicorn>=22.0,<23
requests>=2.31,<3
weaviate-client>=4.6,<5
agno>=0.1
markitdown[docx,pptx,xlsx,pdf]>=0.1,<1
openpyxl>=3.1,<4  # XLSX export for elk_search tool
trafilatura>=1.0,<2
beautifulsoup4>=4.12,<5
lxml>=5.0,<6
python-nmap>=0.7,<1
mcp>=1.0,<2
rank_bm25>=0.2.2,<1  # BM25 ranking for search_tools meta-tool
openai>=1.0,<3
ollama>=0.4,<1
minio>=7.2,<8
ldap3>=2.9,<3
pyotp>=2.9,<3
qrcode[pil]>=7.4,<8
jsonschema>=4.22,<5
pyshark>=0.6,<1
pikepdf>=9.0,<10
python-docx>=1.1,<2
markdown>=3.7,<4
jinja2>=3.1,<4
# gssapi>=1.8  # optional: Kerberos/GSSAPI support (uncomment to enable KERBEROS auth method)
google-genai
checkdmarc>=5.14,<6
pydnsbl>=1.1,<2
zeep[async]>=4.2,<5
nvdlib>=0.7,<1           # NVD (National Vulnerability Database) API client for CVE/CPE lookups
python-gvm>=24,<28       # Greenbone Vulnerability Manager Protocol (GMP) client — OpenVAS / GVM (used by openvas_* tools)
geoip2>=4.8,<6           # MaxMind GeoLite2 / GeoIP2 database reader (GeoLite2-ASN.mmdb)
IP2Location>=8.10,<9     # IP2Location BIN database reader (IP2LOCATION-LITE-DB11.IPV6.BIN)
zabbix-utils>=2.0.2,<3   # Official Zabbix SIA Python API client (used by zabbix_query tool)
kubernetes>=30,<32       # Official Kubernetes Python client (used by k8s_* tools)
# Azure SDK (used by azure_* tools — async via the .aio submodules)
azure-identity>=1.19,<2
azure-mgmt-resource>=23,<25
azure-mgmt-compute>=33,<35
azure-mgmt-network>=27,<29
azure-mgmt-monitor>=6,<7
azure-mgmt-costmanagement>=4,<5
azure-mgmt-advisor>=9,<10
azure-mgmt-sql>=3,<4
azure-mgmt-web>=7,<9
azure-mgmt-containerservice>=33,<35
azure-mgmt-storage>=22,<24
azure-mgmt-subscription>=3,<4
azure-mgmt-devtestlabs>=9,<10  # DevTestLab schedules (auto-shutdown VMs)
pyVmomi>=8.0,<9          # VMware vSphere Web Services SDK (used by vcenter_* tools)
polars[rtcompat]>=1.0,<2 # DataFrame engine used by core CSV tools (csv_describe / csv_query / csv_soc). [rtcompat] picks a build that runs on older CPUs lacking AVX2/FMA (avoids "Missing required CPU features" warning + crash risk).
duckdb>=1.0,<2           # In-process SQL engine used by core CSV tools (csv_query, SELECT-only)
pyarrow>=16,<22          # Required by polars.DataFrame.to_arrow() in csv_query (zero-copy DuckDB register)
chardet>=5.2,<6          # Encoding auto-detection for CSV loader
rt>=3,<4                 # Request Tracker REST 2.0 client used by RT tools (rt.rest2.AsyncRt)
authlib>=1.3,<2          # OIDC id_token validation (Microsoft Entra ID SSO)
# E-goi marketing platform official SDK (used by egoi_* tools). Distributed only via GitHub
# (not on PyPI); pin to a specific commit/tag of E-goi/sdk-python for reproducible builds.
egoi_sdk @ git+https://github.com/guardiankey/egoi_sdk-python.git