<!-- AUTO-GENERATED by scripts/generate_tools_docs.py — do not edit manually. -->
<!-- Source of truth: the tool's Python source file(s) listed at the bottom. -->

# Vcenter Tools

## Overview

gSage AI — High-level VMware vCenter dashboards (read-only summaries).

## Tools in this group

| Tool | Summary | Permissions | Requires approval | Core |
| --- | --- | --- | :---: | :---: |
| `vcenter_dashboard` | VMware vCenter dashboards: cluster overview, host health, VM health, datastore summary, capacity/overcommit report. | `vcenter:read` | — | — |
| `vcenter_inventory` | Read-only VMware vCenter inventory: datacenters, clusters, hosts, VMs + config, templates, datastores, networks, snapshots, tasks/events, find-VM-by-IP, perf metrics. Auto-CSV on >100 rows. | `vcenter:read` | — | — |
| `vcenter_manage` | Approval-gated vCenter management: create-from-template, edit, clone, VM<->template, power, snapshots, vMotion, delete. | `vcenter:write` | ✓ | — |

## Configuration

> The JSON below goes into the **configtool model** in the web UI (per organisation; can also be scoped to a department).

> **Multiple profiles supported** — you may create more than one config row per organisation, each with a distinct `profile_id`. The agent picks one via the `config_profile` parameter at call time. State, rate limits and circuit-breaker counters are isolated per profile.

> **Sensitive fields** (api keys / passwords / tokens) are *omitted* from the JSON below — set them via environment variables (see the _Environment variables_ table) or directly in the encrypted DB row from the web UI.

### Empty configtool template (required fields only)

```json
{
  "host": "",
  "user": "",
  "password": ""
}
```

### Example configtool model

```json
{
  "datacenter": "",
  "host": "",
  "password": "",
  "port": 443,
  "profiles": {},
  "timeout": 60,
  "user": "",
  "verify_ssl": true
}
```

### Field reference

| Field | Type | Required | Sensitive | Default | Description |
| --- | --- | :---: | :---: | --- | --- |
| `datacenter` | `string` | — | — | `""` | Optional default datacenter name used to scope inventory walks when not provided in params. |
| `host` | `string` | ✓ | — | `""` | vCenter Server FQDN or IP address. |
| `password` | `string` | ✓ | — | `""` | vCenter password (sensitive). |
| `port` | `integer` | — | — | `443` | vCenter HTTPS port (default 443). |
| `profiles` | `object` | — | — | `{}` | Additional named vCenters. Each key is a profile name selectable via the 'profile' param; its value accepts the same fields as the top level. The top-level fields define the implicit 'default' vCenter. |
| `timeout` | `integer` | — | — | `60` | Per-call timeout in seconds (default 60). |
| `user` | `string` | ✓ | — | `""` | vCenter username (e.g. 'svc-gsage@vsphere.local'). |
| `verify_ssl` | `boolean` | — | — | `true` | Validate the vCenter TLS certificate (default true). Set false for self-signed lab certificates. |

## Environment variables

The variables below are derived automatically from each tool's `config_schema`. Set them in your `.env` (or via your secrets backend) to seed defaults for every organisation. **Per-org DB config takes precedence over env vars.**

| Variable | Maps to field | Type | Default | Sensitive | Description |
| --- | --- | --- | --- | :---: | --- |
| `TOOL_VCENTER__DATACENTER` | `datacenter` | `string` | `""` | — | Optional default datacenter name used to scope inventory walks when not provided in params. |
| `TOOL_VCENTER__HOST` | `host` | `string` | `""` | — | vCenter Server FQDN or IP address. |
| `TOOL_VCENTER__PASSWORD` | `password` | `string` | `""` | — | vCenter password (sensitive). |
| `TOOL_VCENTER__PORT` | `port` | `integer` | `443` | — | vCenter HTTPS port (default 443). |
| `TOOL_VCENTER__PROFILES` | `profiles` | `object` | `{}` | — | Additional named vCenters. Each key is a profile name selectable via the 'profile' param; its value accepts the same fields as the top level. The top-level fields define the implicit 'default' vCenter. |
| `TOOL_VCENTER__TIMEOUT` | `timeout` | `integer` | `60` | — | Per-call timeout in seconds (default 60). |
| `TOOL_VCENTER__USER` | `user` | `string` | `""` | — | vCenter username (e.g. 'svc-gsage@vsphere.local'). |
| `TOOL_VCENTER__VERIFY_SSL` | `verify_ssl` | `boolean` | `true` | — | Validate the vCenter TLS certificate (default true). Set false for self-signed lab certificates. |
| `TOOL_VCENTER_MANAGE__AUTO_APPROVE` | `auto_approve` | `boolean` | `false` | — | When true, HITL approval requests for this tool are auto-approved by the backend without human review. Useful for lab/dev environments. DB toolconfig overrides env TOOL_<NAME>__AUTO_APPROVE; default is false. |

_Note: any field above can also be overridden per-tool by using the prefix `TOOL_<TOOL_NAME>__` instead of the shared `TOOL_<NAMESPACE>__` — useful when a single tool in the family needs a distinct value._

## Operations

### `vcenter_inventory`

- **`find_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |

- **`get_cluster`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |

- **`get_host`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |

- **`get_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |

- **`get_vm_metrics`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |

- **`list_clusters`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_datacenters`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_datastores`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_folders`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_hosts`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |

- **`list_networks`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_resource_pools`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_snapshots`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |

- **`list_templates`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to query (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | — | Target object name. Required by get_cluster, get_host, get_vm, list_snapshots, get_vm_metrics. For find_vm, matches the VM inventory/DNS name. |
  | `ip` | `string` | — | [find_vm] Guest IP address to resolve to a VM. |
  | `uuid` | `string` | — | [find_vm] VM instance UUID (or BIOS UUID). |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |
  | `max_results` | `integer` | — | Maximum items to return (default 100, hard cap 2000). |
  | `force_refresh` | `boolean` | — | Bypass the Redis cache for this call (still writes the fresh value back). Cache TTL is 300s. |
  | `export_csv` | `boolean` | — | Force CSV artifact even for small results. CSV is generated automatically over 100 rows. |
  | `export_json` | `boolean` | — | Persist the full result as a JSON artifact for programmatic post-processing. |

- **`list_vms`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `cluster` | `string` | — | [list_vms, list_hosts] Filter by cluster name. |
  | `host` | `string` | — | [list_vms] Filter by ESXi host name. |
  | `folder` | `string` | — | [list_vms] Filter by VM folder name. |
  | `power_state` | `string` | — | [list_vms] Filter VMs by power state. |
  | `templates_only` | `boolean` | — | [list_vms] Return only templates (default false). list_vms excludes templates by default. |

- **`recent_events`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |

- **`recent_tasks`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `entity` | `string` | — | [recent_tasks, recent_events] Restrict to a single VM/host/cluster name. Omit for vCenter-wide recent activity. |


### `vcenter_manage`

- **`clone_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |

- **`create_snapshot`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `snapshot_description` | `string` | — | [create_snapshot] Optional snapshot description. |
  | `snapshot_memory` | `boolean` | — | [create_snapshot] Include the VM's memory state (default false). |
  | `quiesce` | `boolean` | — | [create_snapshot] Quiesce the guest filesystem via VMware Tools (default false). |

- **`create_vm_from_template`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |
  | `customize_hostname` | `string` | — | [create_vm_from_template] Guest hostname to apply (triggers Linux/Windows guest customization). |
  | `customize_ip` | `string` | — | [create_vm_from_template] Static IPv4 for the first NIC. Omit for DHCP. Requires customize_netmask + customize_gateway. |
  | `customize_netmask` | `string` | — | [create_vm_from_template] Subnet mask for customize_ip. |
  | `customize_gateway` | `string` | — | [create_vm_from_template] Default gateway for customize_ip. |

- **`delete_snapshot`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `remove_children` | `boolean` | — | [delete_snapshot] Also remove child snapshots (default false). |

- **`delete_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to act on (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `reason` | `string` | ✓ | Free-form justification recorded in the audit log. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |
  | `customize_hostname` | `string` | — | [create_vm_from_template] Guest hostname to apply (triggers Linux/Windows guest customization). |
  | `customize_ip` | `string` | — | [create_vm_from_template] Static IPv4 for the first NIC. Omit for DHCP. Requires customize_netmask + customize_gateway. |
  | `customize_netmask` | `string` | — | [create_vm_from_template] Subnet mask for customize_ip. |
  | `customize_gateway` | `string` | — | [create_vm_from_template] Default gateway for customize_ip. |
  | `num_cpu` | `integer` | — | [edit_vm] New vCPU count. |
  | `memory_mb` | `integer` | — | [edit_vm] New memory size in MB. |
  | `disk_label` | `string` | — | [edit_vm] Label of the disk to grow (e.g. 'Hard disk 1'). |
  | `disk_size_gb` | `integer` | — | [edit_vm] New size in GB for disk_label. Grow only — vSphere forbids shrinking a virtual disk. |
  | `annotation` | `string` | — | [edit_vm] Set the VM notes/annotation field. |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `snapshot_description` | `string` | — | [create_snapshot] Optional snapshot description. |
  | `snapshot_memory` | `boolean` | — | [create_snapshot] Include the VM's memory state (default false). |
  | `quiesce` | `boolean` | — | [create_snapshot] Quiesce the guest filesystem via VMware Tools (default false). |
  | `remove_children` | `boolean` | — | [delete_snapshot] Also remove child snapshots (default false). |

- **`edit_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `num_cpu` | `integer` | — | [edit_vm] New vCPU count. |
  | `memory_mb` | `integer` | — | [edit_vm] New memory size in MB. |
  | `disk_label` | `string` | — | [edit_vm] Label of the disk to grow (e.g. 'Hard disk 1'). |
  | `disk_size_gb` | `integer` | — | [edit_vm] New size in GB for disk_label. Grow only — vSphere forbids shrinking a virtual disk. |
  | `annotation` | `string` | — | [edit_vm] Set the VM notes/annotation field. |

- **`migrate_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |

- **`power_off`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to act on (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `reason` | `string` | ✓ | Free-form justification recorded in the audit log. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |
  | `customize_hostname` | `string` | — | [create_vm_from_template] Guest hostname to apply (triggers Linux/Windows guest customization). |
  | `customize_ip` | `string` | — | [create_vm_from_template] Static IPv4 for the first NIC. Omit for DHCP. Requires customize_netmask + customize_gateway. |
  | `customize_netmask` | `string` | — | [create_vm_from_template] Subnet mask for customize_ip. |
  | `customize_gateway` | `string` | — | [create_vm_from_template] Default gateway for customize_ip. |
  | `num_cpu` | `integer` | — | [edit_vm] New vCPU count. |
  | `memory_mb` | `integer` | — | [edit_vm] New memory size in MB. |
  | `disk_label` | `string` | — | [edit_vm] Label of the disk to grow (e.g. 'Hard disk 1'). |
  | `disk_size_gb` | `integer` | — | [edit_vm] New size in GB for disk_label. Grow only — vSphere forbids shrinking a virtual disk. |
  | `annotation` | `string` | — | [edit_vm] Set the VM notes/annotation field. |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `snapshot_description` | `string` | — | [create_snapshot] Optional snapshot description. |
  | `snapshot_memory` | `boolean` | — | [create_snapshot] Include the VM's memory state (default false). |
  | `quiesce` | `boolean` | — | [create_snapshot] Quiesce the guest filesystem via VMware Tools (default false). |
  | `remove_children` | `boolean` | — | [delete_snapshot] Also remove child snapshots (default false). |

- **`power_on`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to act on (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `reason` | `string` | ✓ | Free-form justification recorded in the audit log. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |
  | `customize_hostname` | `string` | — | [create_vm_from_template] Guest hostname to apply (triggers Linux/Windows guest customization). |
  | `customize_ip` | `string` | — | [create_vm_from_template] Static IPv4 for the first NIC. Omit for DHCP. Requires customize_netmask + customize_gateway. |
  | `customize_netmask` | `string` | — | [create_vm_from_template] Subnet mask for customize_ip. |
  | `customize_gateway` | `string` | — | [create_vm_from_template] Default gateway for customize_ip. |
  | `num_cpu` | `integer` | — | [edit_vm] New vCPU count. |
  | `memory_mb` | `integer` | — | [edit_vm] New memory size in MB. |
  | `disk_label` | `string` | — | [edit_vm] Label of the disk to grow (e.g. 'Hard disk 1'). |
  | `disk_size_gb` | `integer` | — | [edit_vm] New size in GB for disk_label. Grow only — vSphere forbids shrinking a virtual disk. |
  | `annotation` | `string` | — | [edit_vm] Set the VM notes/annotation field. |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `snapshot_description` | `string` | — | [create_snapshot] Optional snapshot description. |
  | `snapshot_memory` | `boolean` | — | [create_snapshot] Include the VM's memory state (default false). |
  | `quiesce` | `boolean` | — | [create_snapshot] Quiesce the guest filesystem via VMware Tools (default false). |
  | `remove_children` | `boolean` | — | [delete_snapshot] Also remove child snapshots (default false). |

- **`reset`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to act on (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `reason` | `string` | ✓ | Free-form justification recorded in the audit log. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |
  | `customize_hostname` | `string` | — | [create_vm_from_template] Guest hostname to apply (triggers Linux/Windows guest customization). |
  | `customize_ip` | `string` | — | [create_vm_from_template] Static IPv4 for the first NIC. Omit for DHCP. Requires customize_netmask + customize_gateway. |
  | `customize_netmask` | `string` | — | [create_vm_from_template] Subnet mask for customize_ip. |
  | `customize_gateway` | `string` | — | [create_vm_from_template] Default gateway for customize_ip. |
  | `num_cpu` | `integer` | — | [edit_vm] New vCPU count. |
  | `memory_mb` | `integer` | — | [edit_vm] New memory size in MB. |
  | `disk_label` | `string` | — | [edit_vm] Label of the disk to grow (e.g. 'Hard disk 1'). |
  | `disk_size_gb` | `integer` | — | [edit_vm] New size in GB for disk_label. Grow only — vSphere forbids shrinking a virtual disk. |
  | `annotation` | `string` | — | [edit_vm] Set the VM notes/annotation field. |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `snapshot_description` | `string` | — | [create_snapshot] Optional snapshot description. |
  | `snapshot_memory` | `boolean` | — | [create_snapshot] Include the VM's memory state (default false). |
  | `quiesce` | `boolean` | — | [create_snapshot] Quiesce the guest filesystem via VMware Tools (default false). |
  | `remove_children` | `boolean` | — | [delete_snapshot] Also remove child snapshots (default false). |

- **`revert_snapshot`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |

- **`suspend`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `profile` | `string` | — | Name of the configured vCenter to act on (a key under the config 'profiles' map). Omit (or 'default') for the primary vCenter. |
  | `name` | `string` | ✓ | Primary target name: the VM/template to act on, or — for create_vm_from_template / clone_vm — the SOURCE template/VM to clone from. |
  | `reason` | `string` | ✓ | Free-form justification recorded in the audit log. |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `datastore` | `string` | — | [create_vm_from_template, clone_vm, migrate_vm] Target datastore. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |
  | `folder` | `string` | — | [create_vm_from_template, clone_vm] Target VM folder. Defaults to the source's folder. |
  | `power_on` | `boolean` | — | [create_vm_from_template, clone_vm] Power on the new VM after creation (default false). |
  | `customize_hostname` | `string` | — | [create_vm_from_template] Guest hostname to apply (triggers Linux/Windows guest customization). |
  | `customize_ip` | `string` | — | [create_vm_from_template] Static IPv4 for the first NIC. Omit for DHCP. Requires customize_netmask + customize_gateway. |
  | `customize_netmask` | `string` | — | [create_vm_from_template] Subnet mask for customize_ip. |
  | `customize_gateway` | `string` | — | [create_vm_from_template] Default gateway for customize_ip. |
  | `num_cpu` | `integer` | — | [edit_vm] New vCPU count. |
  | `memory_mb` | `integer` | — | [edit_vm] New memory size in MB. |
  | `disk_label` | `string` | — | [edit_vm] Label of the disk to grow (e.g. 'Hard disk 1'). |
  | `disk_size_gb` | `integer` | — | [edit_vm] New size in GB for disk_label. Grow only — vSphere forbids shrinking a virtual disk. |
  | `annotation` | `string` | — | [edit_vm] Set the VM notes/annotation field. |
  | `snapshot_name` | `string` | — | [create_snapshot, revert_snapshot, delete_snapshot] Snapshot name. |
  | `snapshot_description` | `string` | — | [create_snapshot] Optional snapshot description. |
  | `snapshot_memory` | `boolean` | — | [create_snapshot] Include the VM's memory state (default false). |
  | `quiesce` | `boolean` | — | [create_snapshot] Quiesce the guest filesystem via VMware Tools (default false). |
  | `remove_children` | `boolean` | — | [delete_snapshot] Also remove child snapshots (default false). |

- **`template_to_vm`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `cluster` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm] Target cluster (its root resource pool is used unless resource_pool is given). |
  | `host` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target ESXi host. |
  | `resource_pool` | `string` | — | [create_vm_from_template, clone_vm, template_to_vm, migrate_vm] Target resource pool (overrides cluster's root pool). |

- **`vm_to_template`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `new_name` | `string` | — | [create_vm_from_template, clone_vm, vm_to_template (clone=true)] Name of the new VM / template to create. |


## Permissions required

| Tool | Permissions |
| --- | --- |
| `vcenter_dashboard` | `vcenter:read` |
| `vcenter_inventory` | `vcenter:read` |
| `vcenter_manage` | `vcenter:write` |

## Operational metadata

| Tool | Version | Rate limit/min | Timeout (s) | Circuit breaker | Background | Multi-config |
| --- | --- | ---: | ---: | :---: | :---: | :---: |
| `vcenter_dashboard` | `1.0.0` | 20 | 180 | ✓ | — | ✓ |
| `vcenter_inventory` | `1.0.0` | 60 | 120 | ✓ | — | ✓ |
| `vcenter_manage` | `1.0.0` | 20 | 600 | ✓ | — | ✓ |

## Source files

- [src/mcp_server/tools/devops/vmware/vcenter_dashboard.py](src/mcp_server/tools/devops/vmware/vcenter_dashboard.py)
- [src/mcp_server/tools/devops/vmware/vcenter_inventory.py](src/mcp_server/tools/devops/vmware/vcenter_inventory.py)
- [src/mcp_server/tools/devops/vmware/vcenter_manage.py](src/mcp_server/tools/devops/vmware/vcenter_manage.py)
