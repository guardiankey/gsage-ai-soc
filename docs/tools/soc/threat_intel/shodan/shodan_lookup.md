<!-- AUTO-GENERATED by scripts/generate_tools_docs.py — do not edit manually. -->
<!-- Source of truth: the tool's Python source file(s) listed at the bottom. -->

# Shodan Lookup

## Overview

gSage AI — Shodan lookup tool (read-only threat intelligence).

## Tool

| Tool | Summary | Permissions | Requires approval | Core |
| --- | --- | --- | :---: | :---: |
| `shodan_lookup` | Shodan lookups: host/service banners & CVEs for an IP, index search, DNS resolve/reverse and domain intel | `threat:intel` | — | — |

## Configuration

> The JSON below goes into the **configtool model** in the web UI (per organisation; can also be scoped to a department).

> **Multiple profiles supported** — you may create more than one config row per organisation, each with a distinct `profile_id`. The agent picks one via the `config_profile` parameter at call time. State, rate limits and circuit-breaker counters are isolated per profile.

> **Sensitive fields** (api keys / passwords / tokens) are *omitted* from the JSON below — set them via environment variables (see the _Environment variables_ table) or directly in the encrypted DB row from the web UI.

### Empty configtool template (required fields only)

```json
{
  "api_key": "<value>"
}
```

### Example configtool model

```json
{
  "api_key": "<value>",
  "timeout": 20
}
```

### Field reference

| Field | Type | Required | Sensitive | Default | Description |
| --- | --- | :---: | :---: | --- | --- |
| `api_key` | `string` | ✓ | ✓ | — | Shodan API key. |
| `timeout` | `integer` | — | — | `20` | HTTP request timeout in seconds (default: 20). |

## Environment variables

The variables below are derived automatically from each tool's `config_schema`. Set them in your `.env` (or via your secrets backend) to seed defaults for every organisation. **Per-org DB config takes precedence over env vars.**

| Variable | Maps to field | Type | Default | Sensitive | Description |
| --- | --- | --- | --- | :---: | --- |
| `TOOL_SHODAN_LOOKUP__API_KEY` | `api_key` | `string` | — | ✓ | Shodan API key. |
| `TOOL_SHODAN_LOOKUP__TIMEOUT` | `timeout` | `integer` | `20` | — | HTTP request timeout in seconds (default: 20). |

## Operations

### `shodan_lookup`

- **`host_info`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `ip` | `string` | — | Target IP address (host_info). |

- **`search`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `query` | `string` | — | Shodan search query (search / count). |
  | `page` | `integer` | — | Result page for search (1 credit/page). |
  | `export_csv` | `boolean` | — | Persist full search results as a CSV artifact. |
  | `export_json` | `boolean` | — | Persist full search results as a JSON artifact. |
  | `group_by` | `array` | — | Columns to summarise (top-N) for search results. |

- **`count`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `query` | `string` | — | Shodan search query (search / count). |

- **`dns_resolve`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `hostnames` | `string` | — | Comma-separated hostnames to resolve (dns_resolve). |

- **`dns_reverse`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `ips` | `string` | — | Comma-separated IPs to reverse-resolve (dns_reverse). |

- **`domain_info`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `domain` | `string` | — | Domain name (domain_info). |

- **`account`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `ip` | `string` | — | Target IP address (host_info). |
  | `query` | `string` | — | Shodan search query (search / count). |
  | `hostnames` | `string` | — | Comma-separated hostnames to resolve (dns_resolve). |
  | `ips` | `string` | — | Comma-separated IPs to reverse-resolve (dns_reverse). |
  | `domain` | `string` | — | Domain name (domain_info). |
  | `page` | `integer` | — | Result page for search (1 credit/page). |
  | `export_csv` | `boolean` | — | Persist full search results as a CSV artifact. |
  | `export_json` | `boolean` | — | Persist full search results as a JSON artifact. |
  | `group_by` | `array` | — | Columns to summarise (top-N) for search results. |
  | `top_n` | `integer` | — | Top-N limit for the summary block. |


## Permissions required

| Tool | Permissions |
| --- | --- |
| `shodan_lookup` | `threat:intel` |

## Operational metadata

| Tool | Version | Rate limit/min | Timeout (s) | Circuit breaker | Background | Multi-config |
| --- | --- | ---: | ---: | :---: | :---: | :---: |
| `shodan_lookup` | `1.0.0` | 30 | 25 | ✓ | — | ✓ |

## Source files

- [src/mcp_server/tools/soc/threat_intel/shodan/shodan_lookup.py](src/mcp_server/tools/soc/threat_intel/shodan/shodan_lookup.py)
