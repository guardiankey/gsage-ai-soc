<!-- AUTO-GENERATED by scripts/generate_tools_docs.py — do not edit manually. -->
<!-- Source of truth: the tool's Python source file(s) listed at the bottom. -->

# Censys Lookup

## Overview

gSage AI — Censys lookup tool (read-only threat intelligence).

## Tool

| Tool | Summary | Permissions | Requires approval | Core |
| --- | --- | --- | :---: | :---: |
| `censys_lookup` | Censys lookups: host detail for an IP, hosts search (Censys query language), DNS names and field aggregation | `threat:intel` | — | — |

## Configuration

> The JSON below goes into the **configtool model** in the web UI (per organisation; can also be scoped to a department).

> **Multiple profiles supported** — you may create more than one config row per organisation, each with a distinct `profile_id`. The agent picks one via the `config_profile` parameter at call time. State, rate limits and circuit-breaker counters are isolated per profile.

> **Sensitive fields** (api keys / passwords / tokens) are *omitted* from the JSON below — set them via environment variables (see the _Environment variables_ table) or directly in the encrypted DB row from the web UI.

### Empty configtool template (required fields only)

```json
{
  "api_id": "<value>",
  "api_secret": "<value>"
}
```

### Example configtool model

```json
{
  "api_id": "<value>",
  "api_secret": "<value>",
  "base_url": "<value>",
  "timeout": 20
}
```

### Field reference

| Field | Type | Required | Sensitive | Default | Description |
| --- | --- | :---: | :---: | --- | --- |
| `api_id` | `string` | ✓ | ✓ | — | Censys API ID (HTTP Basic username). |
| `api_secret` | `string` | ✓ | ✓ | — | Censys API Secret (HTTP Basic password). |
| `base_url` | `string` | — | — | — | Override the Censys API base URL. Defaults to https://search.censys.io/api/v2. Set this only if your account is on a different Censys host. |
| `timeout` | `integer` | — | — | `20` | HTTP request timeout in seconds (default: 20). |

## Environment variables

The variables below are derived automatically from each tool's `config_schema`. Set them in your `.env` (or via your secrets backend) to seed defaults for every organisation. **Per-org DB config takes precedence over env vars.**

| Variable | Maps to field | Type | Default | Sensitive | Description |
| --- | --- | --- | --- | :---: | --- |
| `TOOL_CENSYS_LOOKUP__API_ID` | `api_id` | `string` | — | ✓ | Censys API ID (HTTP Basic username). |
| `TOOL_CENSYS_LOOKUP__API_SECRET` | `api_secret` | `string` | — | ✓ | Censys API Secret (HTTP Basic password). |
| `TOOL_CENSYS_LOOKUP__BASE_URL` | `base_url` | `string` | — | — | Override the Censys API base URL. Defaults to https://search.censys.io/api/v2. Set this only if your account is on a different Censys host. |
| `TOOL_CENSYS_LOOKUP__TIMEOUT` | `timeout` | `integer` | `20` | — | HTTP request timeout in seconds (default: 20). |

## Operations

### `censys_lookup`

- **`host_view`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `ip` | `string` | — | Target IP address (host_view, host_names). |

- **`host_search`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `query` | `string` | — | Censys Search query (host_search, aggregate). |
  | `per_page` | `integer` | — | Results per page for host_search (max 100). |
  | `cursor` | `string` | — | Pagination cursor returned by a previous host_search. |

- **`host_names`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `ip` | `string` | — | Target IP address (host_view, host_names). |

- **`aggregate`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `query` | `string` | — | Censys Search query (host_search, aggregate). |
  | `field` | `string` | — | Field to aggregate on, e.g. 'services.port' (aggregate). |
  | `num_buckets` | `integer` | — | Number of aggregation buckets to return (aggregate). |


## Permissions required

| Tool | Permissions |
| --- | --- |
| `censys_lookup` | `threat:intel` |

## Operational metadata

| Tool | Version | Rate limit/min | Timeout (s) | Circuit breaker | Background | Multi-config |
| --- | --- | ---: | ---: | :---: | :---: | :---: |
| `censys_lookup` | `1.0.0` | 30 | 25 | ✓ | — | ✓ |

## Source files

- [src/mcp_server/tools/soc/threat_intel/censys/censys_lookup.py](src/mcp_server/tools/soc/threat_intel/censys/censys_lookup.py)
