<!-- AUTO-GENERATED by scripts/generate_tools_docs.py — do not edit manually. -->
<!-- Source of truth: the tool's Python source file(s) listed at the bottom. -->

# Wazuh Query

## Overview

gSage AI — Wazuh read-only query tool.

## Tool

| Tool | Summary | Permissions | Requires approval | Core |
| --- | --- | --- | :---: | :---: |
| `wazuh_query` | Query Wazuh: agent inventory & status, manager health, detection rules, SCA and File Integrity Monitoring results | `wazuh:read` | — | — |

## Configuration

> The JSON below goes into the **configtool model** in the web UI (per organisation; can also be scoped to a department).

> **Multiple profiles supported** — you may create more than one config row per organisation, each with a distinct `profile_id`. The agent picks one via the `config_profile` parameter at call time. State, rate limits and circuit-breaker counters are isolated per profile.

> **Sensitive fields** (api keys / passwords / tokens) are *omitted* from the JSON below — set them via environment variables (see the _Environment variables_ table) or directly in the encrypted DB row from the web UI.

### Empty configtool template (required fields only)

```json
{
  "url": "<value>",
  "username": "<value>",
  "password": "<value>"
}
```

### Example configtool model

```json
{
  "password": "<value>",
  "timeout": 30,
  "url": "<value>",
  "username": "<value>",
  "verify_tls": true
}
```

### Field reference

| Field | Type | Required | Sensitive | Default | Description |
| --- | --- | :---: | :---: | --- | --- |
| `password` | `string` | ✓ | ✓ | — | Wazuh API password. |
| `timeout` | `integer` | — | — | `30` | HTTP request timeout in seconds (default: 30). |
| `url` | `string` | ✓ | — | — | Wazuh Manager API base URL including port, e.g. https://wazuh.example.com:55000. Must be reachable from the mcp-server container. |
| `username` | `string` | ✓ | — | — | Wazuh API user (e.g. 'wazuh' or 'wazuh-wui'). |
| `verify_tls` | `boolean` | — | — | `true` | Verify the server TLS certificate (default: true). Wazuh ships a self-signed certificate by default, so set this to false for out-of-the-box deployments. |

## Environment variables

The variables below are derived automatically from each tool's `config_schema`. Set them in your `.env` (or via your secrets backend) to seed defaults for every organisation. **Per-org DB config takes precedence over env vars.**

| Variable | Maps to field | Type | Default | Sensitive | Description |
| --- | --- | --- | --- | :---: | --- |
| `TOOL_WAZUH_QUERY__PASSWORD` | `password` | `string` | — | ✓ | Wazuh API password. |
| `TOOL_WAZUH_QUERY__TIMEOUT` | `timeout` | `integer` | `30` | — | HTTP request timeout in seconds (default: 30). |
| `TOOL_WAZUH_QUERY__URL` | `url` | `string` | — | — | Wazuh Manager API base URL including port, e.g. https://wazuh.example.com:55000. Must be reachable from the mcp-server container. |
| `TOOL_WAZUH_QUERY__USERNAME` | `username` | `string` | — | — | Wazuh API user (e.g. 'wazuh' or 'wazuh-wui'). |
| `TOOL_WAZUH_QUERY__VERIFY_TLS` | `verify_tls` | `boolean` | `true` | — | Verify the server TLS certificate (default: true). Wazuh ships a self-signed certificate by default, so set this to false for out-of-the-box deployments. |

## Operations

### `wazuh_query`

- **`agents_list`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `status` | `string` | — | Filter agents by connection status (agents_list). |
  | `group` | `string` | — | Filter agents (agents_list) or rules (rules_list) by group. |

- **`agent_details`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |

- **`agents_summary`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |
  | `status` | `string` | — | Filter agents by connection status (agents_list). |
  | `group` | `string` | — | Filter agents (agents_list) or rules (rules_list) by group. |
  | `level` | `integer` | — | Minimum rule level filter (rules_list). |
  | `search` | `string` | — | Free-text search filter (name/IP, rule text, file path). |
  | `limit` | `integer` | — | Maximum number of rows to fetch (default 100). |
  | `offset` | `integer` | — | Pagination offset (default 0). |
  | `export_csv` | `boolean` | — | Persist the full result set as a CSV artifact. |
  | `export_json` | `boolean` | — | Persist the full result set as a JSON artifact. |
  | `group_by` | `array` | — | Columns to summarise (top-N counts) for list actions. |
  | `top_n` | `integer` | — | Top-N limit for the summary block. |

- **`groups_list`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |
  | `status` | `string` | — | Filter agents by connection status (agents_list). |
  | `group` | `string` | — | Filter agents (agents_list) or rules (rules_list) by group. |
  | `level` | `integer` | — | Minimum rule level filter (rules_list). |
  | `search` | `string` | — | Free-text search filter (name/IP, rule text, file path). |
  | `limit` | `integer` | — | Maximum number of rows to fetch (default 100). |
  | `offset` | `integer` | — | Pagination offset (default 0). |
  | `export_csv` | `boolean` | — | Persist the full result set as a CSV artifact. |
  | `export_json` | `boolean` | — | Persist the full result set as a JSON artifact. |
  | `group_by` | `array` | — | Columns to summarise (top-N counts) for list actions. |
  | `top_n` | `integer` | — | Top-N limit for the summary block. |

- **`manager_status`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |
  | `status` | `string` | — | Filter agents by connection status (agents_list). |
  | `group` | `string` | — | Filter agents (agents_list) or rules (rules_list) by group. |
  | `level` | `integer` | — | Minimum rule level filter (rules_list). |
  | `search` | `string` | — | Free-text search filter (name/IP, rule text, file path). |
  | `limit` | `integer` | — | Maximum number of rows to fetch (default 100). |
  | `offset` | `integer` | — | Pagination offset (default 0). |
  | `export_csv` | `boolean` | — | Persist the full result set as a CSV artifact. |
  | `export_json` | `boolean` | — | Persist the full result set as a JSON artifact. |
  | `group_by` | `array` | — | Columns to summarise (top-N counts) for list actions. |
  | `top_n` | `integer` | — | Top-N limit for the summary block. |

- **`manager_info`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |
  | `status` | `string` | — | Filter agents by connection status (agents_list). |
  | `group` | `string` | — | Filter agents (agents_list) or rules (rules_list) by group. |
  | `level` | `integer` | — | Minimum rule level filter (rules_list). |
  | `search` | `string` | — | Free-text search filter (name/IP, rule text, file path). |
  | `limit` | `integer` | — | Maximum number of rows to fetch (default 100). |
  | `offset` | `integer` | — | Pagination offset (default 0). |
  | `export_csv` | `boolean` | — | Persist the full result set as a CSV artifact. |
  | `export_json` | `boolean` | — | Persist the full result set as a JSON artifact. |
  | `group_by` | `array` | — | Columns to summarise (top-N counts) for list actions. |
  | `top_n` | `integer` | — | Top-N limit for the summary block. |

- **`rules_list`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `group` | `string` | — | Filter agents (agents_list) or rules (rules_list) by group. |
  | `level` | `integer` | — | Minimum rule level filter (rules_list). |

- **`sca_results`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |

- **`fim_results`** — _(no description)_

  | Parameter | Type | Required | Description |
  | --- | --- | :---: | --- |
  | `agent_id` | `string` | — | Agent ID (e.g. '001'). Required for 'agent_details', 'sca_results' and 'fim_results'. |


## Permissions required

| Tool | Permissions |
| --- | --- |
| `wazuh_query` | `wazuh:read` |

## Operational metadata

| Tool | Version | Rate limit/min | Timeout (s) | Circuit breaker | Background | Multi-config |
| --- | --- | ---: | ---: | :---: | :---: | :---: |
| `wazuh_query` | `1.0.0` | 60 | 30 | ✓ | — | ✓ |

## Source files

- [src/mcp_server/tools/soc/monitoring/wazuh/wazuh_query.py](src/mcp_server/tools/soc/monitoring/wazuh/wazuh_query.py)
