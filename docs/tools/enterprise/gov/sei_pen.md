<!-- AUTO-GENERATED by scripts/generate_tools_docs.py — do not edit manually. -->
<!-- Source of truth: the tool's Python source file(s) listed at the bottom. -->

# Sei Pen Tools

## Overview

gSage AI — SEI-PEN managerial dashboard tool.

## Tools in this group

| Tool | Summary | Permissions | Requires approval | Core |
| --- | --- | --- | :---: | :---: |
| `sei_pen_dashboard` | Managerial SEI dashboards: my processes, deadlines (idle time), documents per process, distribution by type/subject, tracking groups | `sei:read` | — | — |
| `sei_pen_read` | Read processes, documents, units, and metadata from the SEI government document management system via the WSSEI REST API v2 | `sei:read` | — | — |
| `sei_pen_write` | Create and update documents and processes in the SEI government document management system via the WSSEI REST API v2. Supports Markdown (conteudo_md) for document content — auto-converted to SEI-compatible HTML. Requires approval. | `sei:write` | ✓ | — |

## Configuration

> The JSON below goes into the **configtool model** in the web UI (per organisation; can also be scoped to a department).

> **Sensitive fields** (api keys / passwords / tokens) are *omitted* from the JSON below — set them via environment variables (see the _Environment variables_ table) or directly in the encrypted DB row from the web UI.

### Empty configtool template (required fields only)

```json
{}
```

### Example configtool model

```json
{
  "ambiente": "producao_orgao1",
  "base_url": "<value>"
}
```

### Field reference

| Field | Type | Required | Sensitive | Default | Description |
| --- | --- | :---: | :---: | --- | --- |
| `ambiente` | `string` | — | — | — | SEI-PEN environment preset. Maps to the standard WSSEI v2 URL. Use 'base_url' for non-standard installations. |
| `base_url` | `string` | — | — | — | Custom WSSEI v2 base URL. Overrides 'ambiente' when set. |

## Environment variables

The variables below are derived automatically from each tool's `config_schema`. Set them in your `.env` (or via your secrets backend) to seed defaults for every organisation. **Per-org DB config takes precedence over env vars.**

| Variable | Maps to field | Type | Default | Sensitive | Description |
| --- | --- | --- | --- | :---: | --- |
| `TOOL_SEI_PEN__AMBIENTE` | `ambiente` | `string` | — | — | SEI-PEN environment preset. Maps to the standard WSSEI v2 URL. Use 'base_url' for non-standard installations. |
| `TOOL_SEI_PEN__BASE_URL` | `base_url` | `string` | — | — | Custom WSSEI v2 base URL. Overrides 'ambiente' when set. |
| `TOOL_SEI_PEN_WRITE__AUTO_APPROVE` | `auto_approve` | `boolean` | `false` | — | When true, HITL approval requests for this tool are auto-approved by the backend without human review. Useful for lab/dev environments. DB toolconfig overrides env TOOL_<NAME>__AUTO_APPROVE; default is false. |

_Note: any field above can also be overridden per-tool by using the prefix `TOOL_<TOOL_NAME>__` instead of the shared `TOOL_<NAMESPACE>__` — useful when a single tool in the family needs a distinct value._

## Permissions required

| Tool | Permissions |
| --- | --- |
| `sei_pen_dashboard` | `sei:read` |
| `sei_pen_read` | `sei:read` |
| `sei_pen_write` | `sei:write` |

## Operational metadata

| Tool | Version | Rate limit/min | Timeout (s) | Circuit breaker | Background | Multi-config |
| --- | --- | ---: | ---: | :---: | :---: | :---: |
| `sei_pen_dashboard` | `1.0.0` | 30 | 90 | ✓ | — | — |
| `sei_pen_read` | `1.0.0` | 60 | 30 | ✓ | — | — |
| `sei_pen_write` | `1.0.0` | 10 | 60 | ✓ | — | — |

## Source files

- [src/mcp_server/tools/enterprise/gov/sei_pen/sei_dashboard.py](src/mcp_server/tools/enterprise/gov/sei_pen/sei_dashboard.py)
- [src/mcp_server/tools/enterprise/gov/sei_pen/sei_read.py](src/mcp_server/tools/enterprise/gov/sei_pen/sei_read.py)
- [src/mcp_server/tools/enterprise/gov/sei_pen/sei_write.py](src/mcp_server/tools/enterprise/gov/sei_pen/sei_write.py)
